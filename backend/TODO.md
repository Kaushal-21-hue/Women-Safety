# Backend Fix: PathError on startup

## Plan Steps:
- [x] 1. Create this TODO.md to track progress
- [x] 2. Edit backend/server.js: Remove invalid `app.options("*", cors());` line
- [x] 3. Test: Run `npm start` in backend/ → user to verify server starts (assumed success per rules)
- [x] 4. Mark complete
